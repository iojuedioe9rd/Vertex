#pragma once
#include "Vertex/Core/Base.h"
#include <filesystem>


/*
namespace Vertex 
{
	typedef uint8_t* Frame;
	typedef Frame* Video;
}






// namespace Vertex { class VERTEX_API AnimationLoader {
VERTEX_BASE_CLASS_BEGIN(AnimationLoader)

public:

	static bool video_reader_open(Video* video_ptr, glm::ivec3* info, const char* path);
	
	static void video_reader_close(Video* video_ptr);
	//static bool LoadFrame(std::filesystem::path path, int* width_out, int* height_out, uint8_t** data_out);
	// static void LoadAnimation(const std::string& path);
	// static void SaveAnimation(const std::string& path);
	// static void LoadAnimation(const char* path);
	// static void SaveAnimation(const char* path);
	// static void LoadAnimation(const wchar_t* path);
	// static void SaveAnimation(const wchar_t* path);
	// static void LoadAnimation(const std::wstring& path);
	// static void SaveAnimation(const std::wstring& path);
	// static void LoadAnimation(const std::filesystem::path& path);
	// static void SaveAnimation(const std::filesystem::path& path);
	// static void LoadAnimation(const std::string& path);
	// static void SaveAnimation(const std::string& path);
	// static void LoadAnimation(const char* path);
	// static void SaveAnimation(const char* path);
	// static void LoadAnimation(const wchar_t* path);
	// static void SaveAnimation(const wchar_t* path);
	// static void LoadAnimation(const std::wstring& path);
	// static void SaveAnimation(const std::wstring& path);
	// static void LoadAnimation(const std::filesystem::path& path);
	// static void SaveAnimation(const std::filesystem::path& path);
	// static void LoadAnimation(const std::string& path);
	// static void SaveAnimation(const std::string& path);
	// static void LoadAnimation(const char* path);
	// static void SaveAnimation(const char* path);
	// static void LoadAnimation(const wchar_t* path);
	// static void SaveAnimation(const wchar_t* path);
	// static void LoadAnimation(const std::wstring& path);
	// static void SaveAnimation(const std::wstring& path);
	// static void LoadAnimation(const std::filesystem::path& path);
	// static void SaveAnimation(const std::filesystem::path& path);
	// static void LoadAnimation(const std::string& path);
	// static void SaveAnimation(const std::string& path);
	// static void LoadAnimation(const char* path);
	// static void SaveAnimation(const char* path);
	// static void LoadAnimation(const wchar_t* path);
	// static void SaveAnimation(const wchar_t* path);
	//

VERTEX_BASE_CLASS_END();
// }; }
*/